// rest parameters
// so yahan pr c ek rest parameter hai
function myFunc(a,b,...c){
    console.log(`a is ${a}`);
    console.log(`a is ${b}`);
    console.log(`c is`, c);
}

myFunc(1,2,3,45,5,4,5);